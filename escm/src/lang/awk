# awk - configurations for the Awk language
awk -f /dev/stdin
# init
BEGIN {
# assign
@VAR@ = @VAL@;
# string
printf "@STR@";
# display
printf (@EXP@);
# format
printf("%@FMT@", (@EXP@));
# finish
 exit;
}
