;; scm - configurations for the Scheme language
scm -q
;; init
(let ()
;; nil
#f
;; bind
(define @VAR@ @VAL@)
;; string
(display "@STR@")
;; display
(display @EXP@)
;; newline
(newline)
;; finish
(string->symbol ""))
;;
