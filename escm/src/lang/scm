;; scm - configurations for the Scheme language
gosh -b
;; identifier
*lisp*
;; nil
#f
;; bind
(define @VAR@ @VAL@)
;; assign
(set! @VAR@ @VAL@)
;; string
(display "@STR@")
;; display
(display @EXP@)
;; newline
(newline)
;; end
